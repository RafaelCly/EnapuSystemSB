# ========================================
# ARCHIVO DE EJEMPLO DE VARIABLES DE ENTORNO
# ========================================
# 
# Copia este archivo como:
# - `backend/.env` (para configuración del backend)
# - `.env` en la raíz (para configuración del frontend)
#
# IMPORTANTE: No subas archivos .env al repositorio
# ========================================

# ==========================================
# BACKEND (Django) - Guardar en backend/.env
# ==========================================

# Django Settings
SECRET_KEY=django-insecure-CAMBIA-ESTO-POR-UNA-CLAVE-SEGURA-EN-PRODUCCION
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration (PostgreSQL)
DB_NAME=enapu_db
DB_USER=postgres
DB_PASSWORD=TU_CONTRASEÑA_POSTGRESQL_AQUI
DB_HOST=localhost
DB_PORT=5432

# CORS - URLs permitidas desde el frontend
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:8080,http://localhost:8081,http://127.0.0.1:8080,http://127.0.0.1:8081

# ==========================================
# FRONTEND (React + Vite) - Guardar en .env
# ==========================================

VITE_API_URL=http://127.0.0.1:8000/api
